global:
  scrape_interval: 15s

scrape_configs:
  - job_name: node
    static_configs:
      - targets: ['localhost:9100']


remote_write:
  - url: {{ secrets.prometheus_url }}
    basic_auth:
      username: {{ secrets.prometheus_username }}
      password: {{ secrets.grafana_api_key }}
